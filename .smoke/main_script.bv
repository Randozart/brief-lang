// 2026-08-05 (Phase 6): no `main` in Briev — the entry is the first-firing node.
let done: Bool = false;

node start [done == false][done == true] {
    println!("main ran");
    done = true;
    term;
};
