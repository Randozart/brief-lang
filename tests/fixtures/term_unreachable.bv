let a: Int = 0;

// Unreachable: `term! ->` always terminates the transaction (interpreter
// returns TermReturn and unwinds), so Print#(2) can never run.
node n [a == 0][a == 1] {
  a = 1;
  endprogram Print#(a);
  Print#(2);
};
