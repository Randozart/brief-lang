// Multiple state variables, multiple transactions
let count: Int = 0;
let active: Bool = false;

sync<multifield> node increment [count < 100 && active == false][true] {
  count = count + 1;
  term;
};

sync<multifield> node toggle [active == false][true] {
  active = true;
  term;
};
