// HW validation fixture: accumulate b into a every cycle until the guard
// refuses. Pre/post pair is CONSISTENT: one application under [a < 87]
// commits at most 89, so [a < 90] holds on every accepted commit.
let a: Int = 0;
let b: Int = 3;

txn sum [a < 87][a < 90] {
  a = a + b;
}
