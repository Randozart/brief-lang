// HW validation fixture: 8-bit counter (Plan 3.5).
// Sized-uint math goes through explicit Int casts (no implicit coercion).
let counter: UInt[8] = 0 as UInt[8];

txn step [counter < 255][counter < 255] {
  counter = ((counter as Int) + 1) as UInt[8];
}
