// Minimal intrinsic test
// println# and sqrt# are native intrinsics — no TOML bindings needed.
// Replaced: frgn println/sqrt (TOML-based) with intrinsic #-calls.

rct txn test_ff [true]] {
    term println#("Hello from intrinsics!");
};
