// Minimal intrinsic test
// println# is a native intrinsic — no TOML bindings needed.

frgn output_written: Bool = false;

rct txn test_ffi [output_written == false][output_written == true] {
    term! -> println#("Hello from intrinsics!");
};
