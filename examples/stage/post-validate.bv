// 2026-07-21: Updated from $(Post) to $(Generated).

$(Generated) {
    // Generated stage: inspect emitted IR text
    Let$funcs = Find$("define dso_local").Count$();
    EmitWarning$("Generated stage: " + $funcs + " functions in IR");
};

defn greet(name: String) -> String {
    term "Hello, " + name;
};

defn main() -> Int {
    let msg: String = greet("Brief");
    term 0;
};
