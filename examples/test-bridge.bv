// Test bridge for GLUE export pipeline
// 2026-07-22: Uses `export defn` syntax (straight keyword, not modifier).

export defn add(a: Int, b: Int) -> Int {
    term a + b;
};

export defn multiply(a: Int, b: Int) -> Int {
    term a * b;
};
