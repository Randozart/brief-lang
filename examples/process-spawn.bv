// process-spawn.bv — Process spawning and environment
// 2026-07-15: Rewritten to use std/os (auto-imported via prelude).
// spawn, spawn_with_output, argv, setenv need further work.

let pid: Int = getpid();
let cwd: Int = getcwd();

defn main() -> Int {
    let success: Int = chdir(to_c_string("/tmp"));
    term 0;
};
