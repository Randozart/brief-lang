// Simple Contract - Minimal Briev example
// Demonstrates: counter with pre/post conditions

let counter: Int = 0;

node increment [counter < 10]
  [counter == @counter + 1]
{
  counter = counter + 1;
  term;
};