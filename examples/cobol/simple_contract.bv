// Simple Contract - Minimal Brief example
// Demonstrates: counter with pre/post conditions

let counter: Int = 0;

rct txn increment [counter < 10]
  [counter == @counter + 1]
{
  counter = counter + 1;
  term;
};