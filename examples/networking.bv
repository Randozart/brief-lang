// networking.bv — Basic socket operations
// 2026-07-15: Rewritten to use std/os (auto-imported via prelude).

defn main() -> Int {
    let sock: Int = socket(2, 1, 0);
    let ret: Int = close(sock);
    term 0;
};
