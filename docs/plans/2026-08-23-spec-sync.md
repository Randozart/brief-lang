# SPEC Sync — Update Plan (pending owner inspection)

**Date:** 2026-08-23
**Status:** DRAFT — awaiting owner review
**Scope:** `spec/SPEC.md` + implementation corrections for `.^Ready`

---

## Change 1 — §8.3 `enum`: variant construction

**Location:** after the enum declaration example (line ~528)

**Add:**

> ### 8.3.1 Variant construction
>
> Variants are constructed by calling the variant name as a function:
>
> ```briev
> term Ok(a / b);
> term Err("division by zero");
> ```
>
> Zero-payload variants (declared without parens) construct with zero
> arguments:
>
> ```briev
> Null()
> ```
>
> Multi-payload variants accept positional arguments matching the declared
> payload types; they are stored as a Tuple payload internally.
>
> User-defined functions shadow variants — if a `defn` shares a name with a
> variant, the function wins. Variant names must be unique across all enums
> for bare construction to be unambiguous.
>
> The typechecker binds the enum's type parameters positionally from payload
> arguments (`Ok(5)` under `Result<T,E>` binds T=Int); remaining params unify
> against the contextual expected type.

---

## Change 2 — §11.3 `match`: block-expression arm bodies

**Location:** after the patterns bullet list (line ~1057)

**Add:**

> Arm bodies may use block expressions with statements and a tail value:
>
> ```briev
> match res {
>     Ok(val) => {
>         let doubled = val * 2;
>         doubled
>     }
>     Err(msg) => { Print#(msg); 0 - 1 }
> };
> ```
>
> A trailing expression without `;` is the block's implicit value.
> Match expressions may nest inside arm bodies, including inside blocks.
>
> Zero-payload variant patterns use `Variant()` (with parens) to distinguish
> from variable binding patterns.

---

## Change 3 — §12.2 spawn/await: lazy execution model

**Location:** after the storage-class examples (line ~1290)

**Add:**

> ### Execution model
>
> Spawn **captures** the function and its evaluated arguments but does NOT
> execute the body. Bodies are split at cancellation points into segments.
> The first `await` triggers round-robin segment execution of all non-Done
> tasks until the target reaches Done.
>
> Deterministic interleaving at yield boundaries: tasks execute one segment
> per scheduling pass, in spawn order. Single-threaded scheduler — no data
> races, no nondeterministic ordering.
>
> `free task` before any await prevents execution entirely (the body never
> runs). After await has started execution, free sets a cancellation flag
> checked at each yield boundary.

---

## Change 4 — §15.2 Operator classes: List concatenation

**Location:** after "Concatenation has no dedicated `++`" paragraph (~line 1430)

**Replace that sentence with:**

> List concatenation uses `+`: both operands must be `List<T>` with matching
> element types; it resolves as an intrinsic binding (`list_concat`). Other
> collection types resolve through an ordinary operation binding.

---

## Change 5 — §9.5 Objects / §17 Reflection: port readiness

**Location:** §9.5 after the obj example (~line 885), AND §17 reflection targets list

**§9.5 add:**

> ### Port semantics
>
> Input ports expose two operations:
>
> - **`port.^Ready`** → Bool — runtime reflection on the port's internal
>   state flag. True when a pending event is observable.
> - **`port.field`** → payload member projection. Falls through to the
>   payload type's declared fields (`damage.amount` where damage is
>   `Event<Damage>`).
>
> Output ports fire via ArrowAssign: `died <- value;` sets the shared slot's
> Ready flag and stores the payload. Wired consumers observe the same slot
> (shared Rc<RefCell<EventSlot>>).
>
> Cells enforce sealing: external references to cell internals fail at
> compile time; only declared ports are externally visible.

**§17 reflection targets list:** add `Ready` alongside Length/Ptr/Size/Bytes/
Alignment/Type as a valid runtime reflection target on Event ports.

### Implementation correction required:

The parser currently routes `damage.Ready` through `Expr::Field`, which the
interpreter special-cases for EventQ receivers. This violates the delimiter
semantic rule — readiness IS runtime reflection.

**Fix:** route `.^Ready` on Event-typed expressions through
`Expr::Reflect(recv, "Ready", Runtime)` instead of `Expr::Field`.
Remove the Field-based `"Ready"` special case from the interpreter's
field-resolution arm. Keep payload projection via Field unchanged.
Migrate examples/tests: `damage.Ready` → `damage.^Ready`.

**Files:** src/parser/expressions.rs (no change needed — .^ already produces
Reflect), src/typechecker/mod.rs (add Ready→Bool in reflect resolution,
remove Field special case), src/interpreter/eval.rs (same swap),
examples/object_ports.bv, tests.

---

## Change 6 — New stdlib functions reference

**Location:** new subsection after §15.3 (or inline mentions)

**Add brief mentions:**
- `char_at(s: String, i: Int) -> Char` — character-level String access
- Process/environment intrinsics: `Spawn#(cmd) -> Int`,
  `SpawnWithOutput#(cmd) -> String`, `SetEnv#(k,v) -> Int`,
  `GetCwd#() -> String`, `ChDir#(path) -> Int`
- `Barrier#()` — workgroup barrier (CPU no-op; SPIR-V OpControlBarrier)

These don't need full documentation — inline mentions in context suffice.

---

## Implementation checklist (after spec edits)

| Step | File | What |
|---|---|---|
| SPEC edits | spec/SPEC.md | Changes 1–6 above |
| .^Ready fix | src/typechecker/mod.rs | Reflect resolution for Event |
| .^Ready fix | src/interpreter/eval.rs | Move Ready check from Field arm to Reflect arm |
| .^Ready fix | examples/object_ports.bv | damage.Ready → damage.^Ready |
| Migrate | lib/std/result.bv | Already migrated ✓ |
| Test | cargo test --lib | Full suite green |

## NOT changing

- §10.1 Contracts — pre-only `[true]` omission vs written tautology already
  correctly described ("Omitted clauses retain implicit provenance")
- §15.4 Precedence — no new operators added
- §19 FFI — process intrinsics use standard frgn/intrinsic paths
