node probe [true][false] {
    let s: String = "abcdef";
    let sub: String = s[1:4];
    let even: String = s[0:5:2];
    let total: Int = subCharCount#(s) * 100 + evenCharCount#(s);
    term Print#(total);
};
