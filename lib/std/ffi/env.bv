// 2026-07-19: FFI declarations for environment variable access.
// These are C runtime helpers in brief_rt.c, not compiler intrinsics.

frgn __getenv_brief(key: String) -> String;
frgn __getenv_int(key: String) -> Int;
