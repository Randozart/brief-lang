// Phase 3: relocated from compiler Intrinsic enum to std/os/ module
// Auto-imported via prelude.

frgn thread_create(fn_ptr: Int, arg: Int) -> Int ;
frgn thread_join(thread: Int) -> Int ;
frgn thread_exit(code: Int) -> Int ;
frgn mutex_lock(mptr: Int) -> Int ;
frgn mutex_unlock(mptr: Int) -> Int ;
frgn condvar_wait(cptr: Int, mptr: Int) -> Int ;
frgn condvar_signal(cptr: Int) -> Int ;
frgn condvar_broadcast(cptr: Int) -> Int ;
