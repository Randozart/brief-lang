// Phase 3: relocated from compiler Intrinsic enum to std/os/ module
// Auto-imported via prelude.

frgn sched_yield() -> Int ;
frgn getpriority(which: Int, who: Int) -> Int ;
frgn setpriority(which: Int, who: Int, prio: Int) -> Int ;
