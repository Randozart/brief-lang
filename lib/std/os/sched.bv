// Scheduling intrinsics
// Phase 3: relocated from compiler Intrinsic enum

inop sched_yield#() -> Int;
inop getpriority#(which: Int, who: Int) -> Int;
inop setpriority#(which: Int, who: Int, prio: Int) -> Int;
