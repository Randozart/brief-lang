// Dynamic linking intrinsics
// Phase 3: relocated from compiler Intrinsic enum

inop dlopen#(filename: Ptr<Byte>) -> Int;
inop dlsym#(handle: Int, symbol: Ptr<Byte>) -> Int;
inop dlclose#(handle: Int) -> Int;
